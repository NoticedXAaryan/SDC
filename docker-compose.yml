services:

  redis:
    image: redis:7-alpine
    command: ["redis-server", "--requirepass", "${REDIS_PASSWORD:-password}"]
    healthcheck: 
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-password}", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - default
      - dokploy-network

  migrator:
    build:
      context: .
      target: builder
    command: ["sh", "-c", "npx tsx scripts/migrate.ts && npx tsx scripts/set-owner.ts"]
    env_file: [.env]
    networks:
      - default
      - dokploy-network

  app:
    build: .
    depends_on:
      migrator:
        condition: service_completed_successfully
      redis: 
        condition: service_healthy
    env_file: [.env]
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.sdc-app.rule=Host(`sdc.aaaryan.space`)"
      - "traefik.http.services.sdc-app.loadbalancer.server.port=3000"
      - "traefik.http.routers.sdc-app.entrypoints=websecure"
      - "traefik.http.routers.sdc-app.tls.certresolver=letsencrypt"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/api/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      - default
      - dokploy-network

  worker:
    build: 
      context: .
      target: worker-runner
    depends_on: 
      redis: 
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
    env_file: [.env]
    restart: unless-stopped
    networks:
      - default
      - dokploy-network

networks:
  dokploy-network:
    external: true
